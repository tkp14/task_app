<div id="favorite-<%= @task.id %>">
  <% if !current_user.nil? && current_user.favorite?(@task) %>
    <%= render 'users/unfavorite' %>
  <% else %>
    <%= render 'users/favorite' %>
  <% end %>
</div>
